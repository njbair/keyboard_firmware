enum function_id {
    ID_NONE,
};

enum function_opt {
    OPT_NONE,
};

void keymap_call_function(keyrecord_t *record, uint8_t id, uint8_t opt) {
    /*
    switch (id) {
        case SOME_ID:
            // do stuff
    }
    */
};
